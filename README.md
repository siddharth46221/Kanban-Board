
# Offline Kanban Board

A simple and lightweight **Offline Kanban Board** built using **Vanilla JavaScript, HTML, and CSS**. This application allows users to manage tasks efficiently with **drag-and-drop functionality**, **local storage support**, and a **dark mode** option.

## Features

✅ **Add, Edit, and Delete Tasks** – Easily manage your tasks within different Kanban columns.
✅ **Drag & Drop Support** – Reorder tasks seamlessly between columns.
✅ **Persistent Storage** – Uses **LocalStorage** to save tasks even after closing the browser.
✅ **Dark Mode** – Toggle between light and dark themes for better visibility.
✅ **Responsive Design** – Works smoothly on different screen sizes (mobile, tablet, desktop).

## Technologies Used

- **JavaScript (Vanilla)** – For application logic.
- **HTML5** – Structure of the Kanban board.
- **CSS3** – Styling and responsiveness.

## Installation & Usage

1. **Clone the repository**
   ```sh
   git clone https://github.com/your-username/offline-kanban-board.git
   ```
2. **Open the project folder**
   ```sh
   cd offline-kanban-board
   ```
3. **Open `index.html` in a browser** (No setup required, as it works offline).

## How It Works

1. Add tasks to the "To-Do" column.
2. Drag tasks between columns to track progress.
3. Click a task to edit or delete it.
4. Changes are automatically saved in LocalStorage.
5. Toggle dark mode for a better visual experience.

## Future Enhancements

🔹 **Undo/Redo Functionality** – Revert accidental changes.  
🔹 **Export/Import Tasks** – Save and load tasks as JSON/CSV.  
🔹 **AI Task Categorization** – Suggest task categories using AI.  
🔹 **Multi-User Support** – Enable multiple users with different boards.  


